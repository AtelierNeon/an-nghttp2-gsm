# EXTRA_DIST = README.rst
if(NOT NGHTTP2_WITHOUT_INSTALL_FILES AND NOT NGHTTP2_WITHOUT_INSTALL_ALL)
  install(
    PROGRAMS fetch-ocsp-response
    DESTINATION "${CMAKE_INSTALL_DATADIR}/${CMAKE_PROJECT_NAME}"
  )
endif()
